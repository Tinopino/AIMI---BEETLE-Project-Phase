# Legacy Folder

This folder is preserved for reference only and is not part of the active codebase.

> Use the local Docker setup instead. It supports external and DIAG users and works with nnU-Net v1 and v2.
