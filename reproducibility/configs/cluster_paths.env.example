# Example paths for reproducing the experiments on the Radboud csedu cluster.
# Adjust these paths when using a different account or environment.

export AIMI_GROUP_ROOT="/vol/csedu-nobackup/course/IMC037_aimi/group14"
export nnUNet_results="$AIMI_GROUP_ROOT/nnunet/tijn/pathology/nnUNet_results"
export nnUNet_preprocessed="$AIMI_GROUP_ROOT/nnunet/tijn/pathology/nnUNet_preprocessed"

# Activate the original Python environment before submitting jobs:
# source /scratch/tijnveldwijk/virtual_environments/AIMI-BEETLE/bin/activate
