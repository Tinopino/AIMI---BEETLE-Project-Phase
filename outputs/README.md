# Generated outputs

This folder is intentionally empty in Git.

Running the pipeline creates local subfolders such as:

```text
outputs/
├── visuals/
├── external_predictions/
└── submissions/
```

Model checkpoints remain under the configured `nnUNet_results` directory
because they are large generated artifacts.
